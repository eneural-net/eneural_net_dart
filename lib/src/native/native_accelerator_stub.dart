import 'native_backend.dart';

/// Web / unsupported-platform stub: native acceleration is never available.
NativeAccelerator? resolveNativeAccelerator({
  required NativeBackend requested,
  required NativeNetworkSpec spec,
}) => null;
